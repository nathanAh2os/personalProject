//Used to do early testing

//import firebase from '@/firebase/initFirebase';

//I want a piece of information that can be shared by multiple instances, hence the vue state
export const store = {
    state: {
        currentUser: null,
        showMenu: false,
        showSignOut: false
    }
}; 
